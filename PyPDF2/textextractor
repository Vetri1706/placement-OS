import fitz # PyMuPDF

def extract_text_pymupdf(pdf_path, output_txt_path):
    """Extracts text from a PDF file using PyMuPDF and saves it to a TXT file."""
    doc = fitz.open(pdf_path)
    with open(output_txt_path, 'w', encoding='utf-8') as output_file:
        for page_num, page in enumerate(doc):
            text = page.get_text()
            if text:
                output_file.write(f"--- Page {page_num + 1} ---\n")
                output_file.write(text)
                output_file.write("\n\n")
    doc.close()

# Example usage:
extract_text_pymupdf('/home/ghostofsparta/Downloads/WhatSie/Resume-7.pdf', 'output_pymupdf.txt')
