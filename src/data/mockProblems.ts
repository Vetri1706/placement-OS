import type { Problem } from "@/types/problems";

export const mockProblems: Problem[] = [
  {
    id: "two-sum",
    title: "Two Sum",
    difficulty: "Easy",
    tags: ["Array", "HashMap"],
    points: 50,
    description: "Find indices of two numbers that add up to a target.",
    fullDescription:
      "Given an array of integers nums and an integer target, return indices of the two numbers such that they add up to target. Assume exactly one solution exists and you cannot use the same element twice.",
    constraints: ["2 <= nums.length <= 10^4", "-10^9 <= nums[i] <= 10^9", "-10^9 <= target <= 10^9"],
    examples: [
      { input: "nums = [2,7,11,15], target = 9", output: "[0,1]" },
      { input: "nums = [3,2,4], target = 6", output: "[1,2]" },
    ],
    hints: ["Track visited numbers in a map.", "For each number x, check if target - x already exists."],
    editorial: "Use a hash map from value to index to get O(n) time and O(n) space.",
    detailedExplanation: "The key insight is that for each number x in the array, we need to find if (target - x) exists. A brute-force approach checks all pairs in O(n²). Instead, we use a hash map to store each number's index as we iterate. For each new number, we check if its complement (target - current) is already in the map. If yes, we found our answer. This gives us O(n) time with a single pass.",
    approach: "Hash Map / Two-pass or One-pass\n\n1. Create an empty hash map (value → index)\n2. Iterate through the array\n3. For each element, compute complement = target - nums[i]\n4. If complement exists in map, return [map[complement], i]\n5. Otherwise, store nums[i] → i in the map",
    timeComplexity: "O(n) — single pass through the array",
    spaceComplexity: "O(n) — hash map stores up to n entries",
    relatedTopics: ["Hash Table", "Array"],
    solveRate: 79,
    estimatedMinutes: 20,
    popularity: 98,
    createdAt: "2026-01-03",
    starterCode: "function solve(input) {\n  const { nums, target } = input;\n  // Return indices of two numbers that add up to target\n  \n}",
    testCases: [
      { input: '{"nums":[2,7,11,15],"target":9}', expected: "[0,1]" },
      { input: '{"nums":[3,2,4],"target":6}', expected: "[1,2]" },
    ],
  },
  {
    id: "valid-parentheses",
    title: "Valid Parentheses",
    difficulty: "Easy",
    tags: ["Stack", "String"],
    points: 50,
    description: "Determine if brackets are valid and properly nested.",
    fullDescription:
      "Given a string containing just characters '(', ')', '{', '}', '[' and ']', determine if the input string is valid.",
    constraints: ["1 <= s.length <= 10^4", "s consists only of brackets ()[]{}"],
    examples: [{ input: "s = '()[]{}'", output: "true" }],
    hints: ["Use a stack of opening brackets.", "Each closing bracket must match stack top."],
    detailedExplanation: "We use a stack to track opening brackets. When we encounter a closing bracket, we check if the top of the stack has the matching opening bracket. If not, or if the stack is empty when we see a closing bracket, the string is invalid. At the end, the stack must be empty for the string to be valid.",
    approach: "Stack-based matching\n\n1. Create a map of closing→opening brackets\n2. Initialize an empty stack\n3. For each character:\n   - If it's an opening bracket, push to stack\n   - If it's a closing bracket, pop from stack and check if it matches\n4. Return true if stack is empty at the end",
    timeComplexity: "O(n) — single pass through the string",
    spaceComplexity: "O(n) — stack can hold up to n/2 brackets",
    relatedTopics: ["Stack", "String"],
    solveRate: 74,
    estimatedMinutes: 18,
    popularity: 90,
    createdAt: "2026-01-08",
    starterCode:
      "function solve(input) {\n  const { s } = input;\n  // Return true if brackets are valid and properly nested\n  \n}",
    testCases: [
      { input: '{"s":"()[]{}"}', expected: "true" },
      { input: '{"s":"(]"}', expected: "false" },
    ],
  },
  {
    id: "best-time-stock",
    title: "Best Time to Buy and Sell Stock",
    difficulty: "Easy",
    tags: ["Array", "Greedy"],
    points: 50,
    description: "Maximize single-transaction stock profit.",
    fullDescription:
      "Given prices where prices[i] is the price of a stock on day i, maximize your profit by choosing one day to buy and one future day to sell.",
    constraints: ["1 <= prices.length <= 10^5", "0 <= prices[i] <= 10^4"],
    examples: [{ input: "prices = [7,1,5,3,6,4]", output: "5" }],
    hints: ["Track minimum price so far.", "Update best profit at each step."],
    detailedExplanation: "We track the minimum price seen so far as we iterate. At each day, the maximum profit we could get by selling today is (current price - min price so far). We keep updating our best profit. This is a greedy approach — we always buy at the lowest past price and check if selling today beats our record.",
    approach: "Greedy / One-pass\n\n1. Initialize minPrice = Infinity, bestProfit = 0\n2. For each price in the array:\n   - Update minPrice = min(minPrice, price)\n   - Update bestProfit = max(bestProfit, price - minPrice)\n3. Return bestProfit",
    timeComplexity: "O(n) — single pass",
    spaceComplexity: "O(1) — only two variables",
    relatedTopics: ["Array", "Dynamic Programming"],
    solveRate: 72,
    estimatedMinutes: 22,
    popularity: 87,
    createdAt: "2026-01-12",
    starterCode:
      "function solve(input) {\n  const { prices } = input;\n  // Return maximum profit from one buy-sell transaction\n  \n}",
    testCases: [
      { input: '{"prices":[7,1,5,3,6,4]}', expected: "5" },
      { input: '{"prices":[7,6,4,3,1]}', expected: "0" },
    ],
  },
  {
    id: "longest-substring-no-repeat",
    title: "Longest Substring Without Repeating Characters",
    difficulty: "Medium",
    tags: ["String", "Sliding Window", "HashMap"],
    points: 100,
    description: "Find the longest substring with all unique characters.",
    fullDescription:
      "Given a string s, find the length of the longest substring without repeating characters.",
    constraints: ["0 <= s.length <= 5 * 10^4", "s consists of ASCII symbols"],
    examples: [{ input: "s = 'abcabcbb'", output: "3" }],
    hints: ["Use a sliding window.", "Store each character's last seen index."],
    detailedExplanation: "We maintain a sliding window [left, right] where all characters are unique. We use a hash map to store each character's last seen index. When we encounter a duplicate, we move the left pointer past the previous occurrence. At each step, we update the maximum window length.",
    approach: "Sliding Window + Hash Map\n\n1. Initialize left=0, best=0, seen=Map()\n2. For each right from 0 to n-1:\n   - If s[right] is in seen and seen[s[right]] >= left:\n     Move left to seen[s[right]] + 1\n   - Update seen[s[right]] = right\n   - Update best = max(best, right - left + 1)\n3. Return best",
    timeComplexity: "O(n) — each character visited at most twice",
    spaceComplexity: "O(min(n, alphabet)) — hash map size",
    relatedTopics: ["Sliding Window", "String", "Hash Table"],
    solveRate: 63,
    estimatedMinutes: 35,
    popularity: 94,
    createdAt: "2026-01-20",
    starterCode:
      "function solve(input) {\n  const { s } = input;\n  // Return length of longest substring without repeating characters\n  \n}",
    testCases: [
      { input: '{"s":"abcabcbb"}', expected: "3" },
      { input: '{"s":"bbbbb"}', expected: "1" },
    ],
  },
  {
    id: "group-anagrams",
    title: "Group Anagrams",
    difficulty: "Medium",
    tags: ["Array", "HashMap", "Sorting"],
    points: 100,
    description: "Group words that are anagrams of each other.",
    fullDescription:
      "Given an array of strings strs, group the anagrams together. Return the answer in any order.",
    constraints: ["1 <= strs.length <= 10^4", "0 <= strs[i].length <= 100"],
    examples: [{ input: "strs = ['eat','tea','tan','ate','nat','bat']", output: "[['bat'],['nat','tan'],['ate','eat','tea']]" }],
    hints: ["Sort each word and use it as key.", "Collect words by key in a map."],
    detailedExplanation: "Two words are anagrams if they have the same characters in any order. By sorting each word alphabetically, all anagrams produce the same key. We group words by their sorted key using a hash map, then return all groups.",
    approach: "Sort + Hash Map Grouping\n\n1. Create a hash map (sorted_word → list of words)\n2. For each word in input:\n   - Sort its characters to get the key\n   - Add the original word to the map[key] list\n3. Return all values from the map",
    timeComplexity: "O(n × k log k) — n words, k = max word length",
    spaceComplexity: "O(n × k) — storing all words in the map",
    relatedTopics: ["Hash Table", "Sorting", "String"],
    solveRate: 58,
    estimatedMinutes: 34,
    popularity: 84,
    createdAt: "2026-01-23",
    starterCode:
      "function solve(input) {\n  const { strs } = input;\n  // Group anagrams together and return them\n  \n}",
    testCases: [
      { input: '{"strs":["eat","tea","tan","ate","nat","bat"]}', expected: '[["ate","eat","tea"],["bat"],["nat","tan"]]' },
      { input: '{"strs":[""]}', expected: '[[""]]' },
    ],
  },
  {
    id: "coin-change",
    title: "Coin Change",
    difficulty: "Medium",
    tags: ["DP", "Array"],
    points: 100,
    description: "Find the minimum coins needed for a target amount.",
    fullDescription:
      "Given coins of different denominations and a target amount, return the fewest coins needed to make that amount, or -1 if impossible.",
    constraints: ["1 <= coins.length <= 12", "0 <= amount <= 10^4"],
    examples: [{ input: "coins = [1,2,5], amount = 11", output: "3" }],
    hints: ["Use bottom-up dynamic programming.", "dp[x] = min(dp[x], dp[x - coin] + 1)."],
    detailedExplanation: "We build a DP table where dp[i] = minimum coins needed to make amount i. Base case: dp[0] = 0. For each amount from 1 to target, we try every coin denomination. If the coin fits (amount >= coin), we take the minimum of current dp[amount] and dp[amount - coin] + 1. If dp[target] is still Infinity, it's impossible.",
    approach: "Bottom-up Dynamic Programming\n\n1. Create dp array of size (amount+1), fill with Infinity\n2. Set dp[0] = 0\n3. For each value from 1 to amount:\n   - For each coin:\n     If value >= coin: dp[value] = min(dp[value], dp[value-coin] + 1)\n4. Return dp[amount] if finite, else -1",
    timeComplexity: "O(amount × coins) — nested loops",
    spaceComplexity: "O(amount) — dp array",
    relatedTopics: ["Dynamic Programming", "Breadth-First Search"],
    solveRate: 49,
    estimatedMinutes: 45,
    popularity: 76,
    createdAt: "2026-01-29",
    starterCode:
      "function solve(input) {\n  const { coins, amount } = input;\n  // Return minimum number of coins needed, or -1 if impossible\n  \n}",
    testCases: [
      { input: '{"coins":[1,2,5],"amount":11}', expected: "3" },
      { input: '{"coins":[2],"amount":3}', expected: "-1" },
    ],
  },
  {
    id: "top-k-frequent",
    title: "Top K Frequent Elements",
    difficulty: "Medium",
    tags: ["Array", "HashMap", "Heap"],
    points: 100,
    description: "Return the k most frequent elements.",
    fullDescription:
      "Given an integer array nums and integer k, return the k most frequent elements.",
    constraints: ["1 <= nums.length <= 10^5", "k is in range [1, unique(nums)]"],
    examples: [{ input: "nums = [1,1,1,2,2,3], k = 2", output: "[1,2]" }],
    hints: ["Count frequencies first.", "Use bucket sort or heap for top k."],
    detailedExplanation: "First, count the frequency of each element using a hash map. Then we need the k elements with the highest frequency. We can sort by frequency in O(n log n), use a min-heap of size k in O(n log k), or use bucket sort in O(n). Bucket sort: create buckets indexed by frequency, then collect from highest bucket down.",
    approach: "Frequency Count + Sort (or Bucket Sort)\n\n1. Count frequency of each number in a hash map\n2. Sort entries by frequency descending\n3. Return the first k elements\n\nOptimal: Use bucket sort where index = frequency",
    timeComplexity: "O(n log n) with sort, O(n) with bucket sort",
    spaceComplexity: "O(n) — frequency map + buckets",
    relatedTopics: ["Heap", "Bucket Sort", "Hash Table"],
    solveRate: 56,
    estimatedMinutes: 36,
    popularity: 82,
    createdAt: "2026-02-02",
    starterCode:
      "function solve(input) {\n  const { nums, k } = input;\n  // Return the k most frequent elements\n  \n}",
    testCases: [
      { input: '{"nums":[1,1,1,2,2,3],"k":2}', expected: "[1,2]" },
      { input: '{"nums":[1],"k":1}', expected: "[1]" },
    ],
  },
  {
    id: "merge-k-sorted-lists",
    title: "Merge K Sorted Lists",
    difficulty: "Hard",
    tags: ["Linked List", "Heap", "Divide and Conquer"],
    points: 200,
    description: "Merge k sorted linked lists into one sorted list.",
    fullDescription:
      "You are given an array of k linked-lists lists, each linked-list is sorted in ascending order. Merge all the linked-lists into one sorted linked-list and return it.",
    constraints: ["k == lists.length", "0 <= k <= 10^4", "-10^4 <= node.val <= 10^4"],
    examples: [{ input: "lists = [[1,4,5],[1,3,4],[2,6]]", output: "[1,1,2,3,4,4,5,6]" }],
    hints: ["Maintain a min-heap with current heads.", "Pop min and push next node from same list."],
    detailedExplanation: "The naive approach merges all elements and sorts — O(N log N). Better: use a min-heap of size k holding the current head of each list. Pop the smallest, add it to the result, and push the next node from that list. This gives O(N log k). Alternatively, merge lists pairwise using divide and conquer.",
    approach: "Min-Heap / Priority Queue\n\n1. Push the head of each non-empty list into a min-heap\n2. While heap is not empty:\n   - Pop the minimum node\n   - Append it to the merged result\n   - If that node has a next, push next into the heap\n3. Return the merged list",
    timeComplexity: "O(N log k) — N total nodes, k lists",
    spaceComplexity: "O(k) — heap size",
    relatedTopics: ["Heap", "Linked List", "Divide and Conquer"],
    solveRate: 41,
    estimatedMinutes: 55,
    popularity: 75,
    createdAt: "2026-02-05",
    starterCode:
      "function solve(input) {\n  const { lists } = input;\n  // Merge k sorted lists into one sorted list\n  \n}",
    testCases: [
      { input: '{"lists":[[1,4,5],[1,3,4],[2,6]]}', expected: "[1,1,2,3,4,4,5,6]" },
      { input: '{"lists":[]}', expected: "[]" },
    ],
  },
  {
    id: "serialize-binary-tree",
    title: "Serialize and Deserialize Binary Tree",
    difficulty: "Hard",
    tags: ["Tree", "DFS", "BFS", "Design"],
    points: 200,
    description: "Design serialize/deserialize for a binary tree.",
    fullDescription:
      "Design an algorithm to serialize and deserialize a binary tree. There is no restriction on how your algorithm should work.",
    constraints: ["Number of nodes in the tree is in the range [0, 10^4]"],
    examples: [{ input: "root = [1,2,3,null,null,4,5]", output: "[1,2,3,null,null,4,5]" }],
    hints: ["Preorder traversal with null markers works.", "BFS level-order format also valid."],
    detailedExplanation: "Serialization converts the tree to a string. Key insight: preorder traversal with null markers (e.g., 'X') uniquely represents any binary tree. Serialize: visit root, recurse left, recurse right, writing values and 'X' for null nodes. Deserialize: read tokens in order, recursively reconstruct the tree.\n\nAlternatively, use BFS level-order with null markers.",
    approach: "Preorder DFS with null markers\n\nSerialize:\n1. If node is null, write 'X,'\n2. Write node.val + ','\n3. Recurse left, then right\n\nDeserialize:\n1. Split string by ','\n2. If token is 'X', return null\n3. Create node with token value\n4. Recurse to build left and right subtrees",
    timeComplexity: "O(n) — visit every node once",
    spaceComplexity: "O(n) — string storage + recursion stack",
    relatedTopics: ["Tree", "String", "Design"],
    solveRate: 39,
    estimatedMinutes: 60,
    popularity: 70,
    createdAt: "2026-02-10",
    starterCode:
      "function solve(input) {\n  const { root } = input;\n  // Serialize and deserialize the binary tree\n  \n}",
    testCases: [
      { input: '{"root":[1,2,3,null,null,4,5]}', expected: "[1,2,3,null,null,4,5]" },
      { input: '{"root":[]}', expected: "[]" },
    ],
  },
  {
    id: "minimum-window-substring",
    title: "Minimum Window Substring",
    difficulty: "Hard",
    tags: ["String", "Sliding Window", "HashMap"],
    points: 200,
    description: "Find the smallest window containing all target chars.",
    fullDescription:
      "Given two strings s and t, return the minimum window substring of s such that every character in t (including duplicates) is included in the window.",
    constraints: ["1 <= s.length, t.length <= 10^5"],
    examples: [{ input: "s = 'ADOBECODEBANC', t = 'ABC'", output: "BANC" }],
    hints: ["Track required and formed counts.", "Expand right pointer, shrink with left pointer."],
    detailedExplanation: "Use two pointers (sliding window). Expand the right pointer to include characters until all characters from t are covered. Then shrink from the left to find the minimum window. Use two hash maps: one for required character counts (from t) and one for the current window. Track how many unique characters are fully satisfied ('formed' count).",
    approach: "Sliding Window with Two Pointers\n\n1. Build a frequency map 'need' from string t\n2. Initialize left=0, formed=0, window=Map\n3. Expand right pointer:\n   - Add s[right] to window map\n   - If window[ch] == need[ch], increment formed\n   - While formed == need.size (all chars satisfied):\n     Update best window if smaller\n     Remove s[left] from window, adjust formed\n     Move left++\n4. Return the best window substring",
    timeComplexity: "O(|s| + |t|) — each pointer moves at most |s| times",
    spaceComplexity: "O(|s| + |t|) — hash maps",
    relatedTopics: ["Sliding Window", "Hash Table", "String"],
    solveRate: 36,
    estimatedMinutes: 58,
    popularity: 73,
    createdAt: "2026-02-16",
    starterCode:
      "function solve(input) {\n  const { s, t } = input;\n  // Return minimum window substring containing all chars of t\n  \n}",
    testCases: [
      { input: '{"s":"ADOBECODEBANC","t":"ABC"}', expected: '"BANC"' },
      { input: '{"s":"a","t":"aa"}', expected: '""' },
    ],
  },
];
